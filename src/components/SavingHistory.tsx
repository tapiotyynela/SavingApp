import React from 'react';
import { View, Text } from 'react-native'

const SavingHistory = () => {
    return (
        <View style={{ alignItems: 'center', justifyContent: 'center', alignContent: 'center' }}>
            <Text>TALLETUS HISTORIA</Text>
        </View>
    );
};

export default SavingHistory;